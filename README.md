# paper-filter
A local Flask tool that scrapes document links from a web page using requests + BeautifulSoup, downloads each file, extracts text content via Python libraries, and performs keyword-based content search.
